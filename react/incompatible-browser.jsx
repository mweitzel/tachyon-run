var React = require('react')

module.exports = React.createClass({
  render: function() {
    return (
      <div
        id='browser-incompatibility'
        className='browser-incompatibility hidden'
      > This game is only compatible with Chrome for now. </div>
    )
  }
})
